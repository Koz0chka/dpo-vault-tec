# Совместимо с Python 3.12, 3.13 и 3.14.
# Версии выбраны свежие, чтобы у pydantic-core был готовый wheel под cp314
# (старые версии pydantic-core используют PyO3 0.24, который не поддерживает 3.14).
# Диапазоны `>=` позволяют pip взять самую свежую совместимую версию.
fastapi>=0.118.0
uvicorn[standard]>=0.35.0
argon2-cffi>=25.1.0
cryptography>=45.0.7
pydantic>=2.12.0
pydantic-settings>=2.10.1
python-jose[cryptography]>=3.5.0
